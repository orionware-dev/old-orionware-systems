<?xml version="1.0"?>
<project name="OrionCoreAnnotationsServicesBuilder" default="all" basedir=".">
    <property file="CoreAnnotationsServicesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.annotations.services.binaries.dir" value="${orion.core.annotations.services.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.annotations.services.source.dir" value="${orion.core.annotations.services.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	<buildAndPackageProject sourceDir="${orion.core.annotations.services.source.dir}" binariesDir="${orion.core.annotations.services.binaries.dir}" JARPath="${orion.core.annotations.services.jar.file.name}" projectName="${orion.core.annotations.services}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.annotations.services.binaries.dir}" includes="**/**"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.annotations.services.jar.file.name}"/>
    </target>
</project>