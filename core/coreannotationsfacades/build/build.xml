<?xml version="1.0"?>
<project name="OrionCoreAnnotationsFacadesBuilder" default="all" basedir=".">
    <property file="CoreAnnotationsFacadesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.annotations.facades.binaries.dir" value="${orion.core.annotations.facades.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.annotations.facades.source.dir" value="${orion.core.annotations.facades.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	<buildProjectAndPackage sourceDir="${orion.core.annotations.facades.source.dir}" binariesDir="${orion.core.annotations.facades.binaries.dir}" JARPath="${orion.core.annotations.facades.jar.file.name}" projectName="${orion.core.annotations.facades}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.annotations.facades.binaries.dir}" includes="**/**"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.libraries.orionware.libraries.dir}/${orion.core.annotations.facades.jar.file.name}"/>
    </target>
</project>