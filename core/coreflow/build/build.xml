<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreFlowBuilder" default="all" basedir=".">
    <property file="CoreFlowBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.flow.binaries.dir" value="${orion.core.flow.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.flow.source.dir" value="${orion.core.flow.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.core.flow.source.dir}" binariesDir="${orion.core.flow.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.flow.jar.file.name}" projectName="${orion.core.flow}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.flow.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.flow.jar.file.name}"/>
    </target>
</project>