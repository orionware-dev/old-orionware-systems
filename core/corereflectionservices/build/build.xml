<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreReflectionServicesBuilder" default="all" basedir=".">
    <property file="CoreReflectionServicesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.reflection.services.binaries.dir" value="${orion.core.reflection.services.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.reflection.services.source.dir" value="${orion.core.reflection.services.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.core.reflection.services.source.dir}" binariesDir="${orion.core.reflection.services.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.reflection.services.jar.file.name}" projectName="${orion.core.reflection.services}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.reflection.services.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.reflection.services.jar.file.name}"/>
    </target>
</project>