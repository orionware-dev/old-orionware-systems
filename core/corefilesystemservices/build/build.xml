<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreFileSystemServicesBuilder" default="all" basedir=".">
    <property file="CoreFileSystemServicesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.filesystem.services.binaries.dir" value="${orion.core.filesystem.services.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.filesystem.services.source.dir" value="${orion.core.filesystem.services.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.core.filesystem.services.source.dir}" binariesDir="${orion.core.filesystem.services.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.filesystem.services.jar.file.name}" projectName="${orion.core.filesystem.services}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.filesystem.services.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.filesystem.services.jar.file.name}"/>
    </target>
</project>