<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreFlowFacadesBuilder" default="all" basedir=".">
    <property file="CoreFlowFacadesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.flow.facades.binaries.dir" value="${orion.core.flow.facades.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.flow.facades.source.dir" value="${orion.core.flow.facades.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.core.flow.facades.source.dir}" binariesDir="${orion.core.flow.facades.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.flow.facades.jar.file.name}" projectName="${orion.core.flow.facades}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.flow.facades.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.flow.facades.jar.file.name}"/>
    </target>
</project>