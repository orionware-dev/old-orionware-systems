<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreReflectionBuilder" default="all" basedir=".">
    <property file="CoreReflectionBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.reflection.binaries.dir" value="${orion.core.reflection.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.reflection.source.dir" value="${orion.core.reflection.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	    <buildAndPackageProject sourceDir="${orion.core.reflection.source.dir}" binariesDir="${orion.core.reflection.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.reflection.jar.file.name}" projectName="${orion.core.reflection}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.reflection.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.reflection.jar.file.name}"/>
    </target>
</project>