<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreDependencyInjectionServicesBuilder" default="all" basedir=".">
    <property file="CoreDependencyInjectionServicesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.dependencyinjection.services.binaries.dir" value="${orion.core.dependencyinjection.services.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.dependencyinjection.services.source.dir" value="${orion.core.dependencyinjection.services.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.core.dependencyinjection.services.source.dir}" binariesDir="${orion.core.dependencyinjection.services.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.dependencyinjection.services.jar.file.name}" projectName="${orion.core.dependencyinjection.services}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.dependencyinjection.services.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.dependencyinjection.services.jar.file.name}"/>
    </target>
</project>