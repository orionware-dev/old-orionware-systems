<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreFlowServicesBuilder" default="all" basedir=".">
    <property file="CoreFlowServicesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.flow.services.binaries.dir" value="${orion.core.flow.services.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.flow.services.source.dir" value="${orion.core.flow.services.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.core.flow.services.source.dir}" binariesDir="${orion.core.flow.services.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.flow.services.jar.file.name}" projectName="${orion.core.flow.services}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.flow.services.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.flow.services.jar.file.name}"/>
    </target>
</project>