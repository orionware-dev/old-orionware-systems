<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionCoreDependencyInjectionFacadesBuilder" default="all" basedir=".">
    <property file="CoreDependencyInjectionFacadesBuild.prop"/>
    <property name="orion.core.binaries.dir" value="${orion.core.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.dependencyinjection.facades.binaries.dir" value="${orion.core.dependencyinjection.facades.dir}/${default.binaries.dir.name}"/>
    <property name="orion.core.dependencyinjection.facades.source.dir" value="${orion.core.dependencyinjection.facades.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.core.dependencyinjection.facades.source.dir}" binariesDir="${orion.core.dependencyinjection.facades.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.core.dependencyinjection.facades.jar.file.name}" projectName="${orion.core.dependencyinjection.facades}"/>
        
        <copy todir="${orion.core.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.core.dependencyinjection.facades.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.core.dependencyinjection.facades.jar.file.name}"/>
    </target>
</project>