<?xml version="1.0"?>
<project name="OrionDesignPatternsBuilder" default="all" basedir=".">
    <property file="DesignPatternsBuild.prop"/>
    <property name="orion.design.patterns.binaries.dir" value="${orion.design.patterns.dir}/${default.binaries.dir.name}"/>
	<property name="orion.design.patterns.design.patterns.binaries.dir" value="${orion.design.patterns.design.patterns.dir}/${default.binaries.dir.name}"/>
	<property name="orion.design.patterns.design.patterns.source.dir" value="${orion.design.patterns.design.patterns.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	<buildProjectAndPackage sourceDir="${orion.design.patterns.design.patterns.source.dir}" binariesDir="${orion.design.patterns.design.patterns.binaries.dir}" JARPath="${orion.design.patterns.design.patterns.jar.file.name}" projectName="${orion.design.patterns.design.patterns}"/>
    	                
        <copy todir="${orion.design.patterns.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.design.patterns.design.patterns.binaries.dir}" includes="**/**"/>
        </copy>
    </target>
</project>