package webcore.pages;

import java.util.logging.Logger;

//@Controller
//@PropertySource({"classpath:pollplug.properties", "classpath:database.properties", "classpath:security.properties", "classpath:social.properties", "classpath:utilities.properties", "classpath:create-poll-page.properties"})
public class PAGE_CONTROLLER_EXAMPLE
{
    private final static Logger LOGGER = Logger.getLogger(PAGE_CONTROLLER_EXAMPLE.class.getName());
    /*@Inject
    private Environment environment;


    public Environment getEnvironment()
    {
        return environment;
    }


    public void setEnvironment(Environment environment)
    {
        this.environment = environment;
    }*/
}