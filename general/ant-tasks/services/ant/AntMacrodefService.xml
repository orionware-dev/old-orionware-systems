<?xml version="1.0"?>
<project name="AntMacrodefService" basedir=".">
	<macrodef name="importMacrodefsFile">
		<attribute name="dir" default=""/>
		
		<sequential>
            <import>
                <fileset dir="@{fileset}" includes="*.xml"/>
            </import>
        </sequential>
    </macrodef>
</project>