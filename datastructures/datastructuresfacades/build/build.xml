<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionDataStructuresFacadesBuilder" default="all" basedir=".">
    <property file="DataStructuresFacadesBuild.prop"/>
    <property name="orion.data.structures.facades.binaries.dir" value="${orion.data.structures.facades.dir}/${default.binaries.dir.name}"/>
    <property name="orion.data.structures.facades.binaries.dir" value="${orion.data.structure.facadess.dir}/${default.binaries.dir.name}"/>
    <property name="orion.data.structures.facades.source.dir" value="${orion.data.structures.facades.dir}/${default.source.dir.name}"/>

    
    <target name="all">
    	   <buildAndPackageProject sourceDir="${orion.data.structures.facades.source.dir}" binariesDir="${orion.data.structures.facades.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.data.structures.facades.jar.file.name}" projectName="${orion.data.structures.facades}"/>
        
        <copy todir="${orion.data.structures.facades.binaries.dir}" verbose="yes" failonerror="false" overwrite="true">
            <fileset dir="${orion.data.structures.facades.binaries.dir}" includes="${default.any.file.extension.pattern}"/>
        </copy>
    </target>
	
	
	<target name="delete_JAR">
		<deleteFile path="${orion.orionware.libraries.libraries.dir}/${orion.data.structures.facades.jar.file.name}"/>
    </target>
</project>