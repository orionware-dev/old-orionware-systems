<?xml version="1.0" encoding="UTF-8"?>
<project name="OrionFlowBuilder" default="all" basedir=".">
    <property name="orion.flow.source.dir" value="${orion.flow.dir}/${default.source.dir.name}"/>

    
    <target name="all">
        <buildAndPackageProject sourceDir="${orion.flow.source.dir}" binariesDir="${orion.flow.binaries.dir}" JARPath="${orion.orionware.libraries.libraries.dir}/${orion.flow.jar.file.name}" projectName="${orion.flow}"/>
    </target>
</project>